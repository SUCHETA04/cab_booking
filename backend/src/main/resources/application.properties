spring.application.name=backend
server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/cab_booking
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret (Should be moved to env vars later)
jwt.secret=cabBookingSecretKeyThatNeedsToBeLongEnoughForHS256AlgorithmToWorkProperly123!
jwt.expiration=86400000

# Razorpay Configuration
razorpay.api.key=rzp_test_SO4YpaYNVDXVK7
razorpay.api.secret=HDRuYy4OhVd8DM5SwQK7zhrU

# Google OAuth2 (To be configured)
spring.security.oauth2.client.registration.google.client-id=YOUR_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=profile,email
